style.css2

/* GLOBAL RESETS */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    background-color: #1e1e1e;
    color: #ffffff;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    height: 100vh;
    display: flex;
    flex-direction: column;
    overflow: hidden;
}

/* MAIN CONTENT STRUCTURE (Flex Layout) */
.main-container {
    display: flex;
    flex: 1;
    width: 100%;
    overflow: hidden;
}

/* 1. ACTIVITY BAR (Far Left Strip) */
.activity-bar {
    width: 50px;
    background-color: #333333;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: center;
    padding: 10px 0;
}

.top-icons {
    display: flex;
    flex-direction: column;
    gap: 20px;
    width: 100%;
}

.activity-bar .icon {
    font-size: 20px;
    color: #858585;
    cursor: pointer;
    text-align: center;
    width: 100%;
    padding: 8px 0;
}

.activity-bar .icon.active {
    color: #ffffff;
    border-left: 2px solid #007acc;
}

/* 2. SIDEBAR PANELS */
.sidebar {
    width: 220px;
    background-color: #252526;
    border-right: 1px solid #3c3c3c;
    display: flex;
    flex-direction: column;
}

.sidebar-panel {  
    display: none;  
    flex-direction: column;  
    height: 100%;  
}  
  
.sidebar-panel.active {  
    display: flex;  
}  

.sidebar-header {
    padding: 10px 15px;
    font-size: 11px;
    font-weight: bold;
    color: #bbbbbb;
    display: flex;
    justify-content: space-between;
    align-items: center;
    letter-spacing: 0.5px;
}

.file-tree {
    padding: 5px 0;
}

.file-item {
    padding: 6px 20px;
    font-size: 13px;
    color: #cccccc;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 8px;
}

.file-item:hover, .file-item.active {
    background-color: #37373d;
    color: #ffffff;
}
  
.panel-content {  
    padding: 15px;  
    font-size: 13px;  
}  
  
.vs-input {  
    width: 100%;  
    background-color: #3c3c3c;  
    border: 1px solid #6b6b6b;  
    color: white;  
    padding: 4px 8px;  
    outline: none;  
}  

/* 3. MAIN EDITOR AREA */
.editor-area {
    flex: 1;
    display: flex;
    flex-direction: column;
    background-color: #1e1e1e;
}

.tabs-container {
    display: flex;
    background-color: #2d2d2d;
    height: 35px;
}

.tab {
    padding: 0 20px;
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    color: #969696;
    background-color: #2d2d2d;
    border-right: 1px solid #252526;
    cursor: pointer;
    user-select: none;
}

.tab.active {
    background-color: #1e1e1e;
    color: #ffffff;
    border-top: 1px solid #007acc;
}

/* CODE INTERIOR WINDOW */
.code-window {
    display: flex;
    flex: 1;
    padding: 10px 0;
    overflow-y: auto;
}

.line-numbers {
    width: 45px;
    text-align: right;
    padding-right: 12px;
    font-family: 'Consolas', monospace;
    font-size: 14px;
    color: #858585;
    display: flex;
    flex-direction: column;
    user-select: none;
}
  
.code-display {  
    flex: 1;
    outline: none;  
    white-space: pre;  
    color: #d4d4d4;
    font-family: 'Consolas', 'Monaco', 'Courier New', monospace;  
    font-size: 14px;  
    padding-left: 5px;
}  

/* 4. STATUS BAR (Very Bottom) */
.status-bar {
    height: 22px;
    background-color: #007acc;
    color: #ffffff;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 10px;
    font-size: 12px;
    user-select: none;
}

.status-left, .status-right {
    display: flex;
    align-items: center;
    gap: 15px;
}

.status-item {
    display: flex;
    align-items: center;
    gap: 5px;
}
